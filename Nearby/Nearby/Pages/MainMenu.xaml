<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Nearby.Pages.MainMenu"
             xmlns:control="clr-namespace:Nearby.Controls"
             Title="{Binding Title}"
             BackgroundColor="{DynamicResource WindowBackgroundTable}">

  <ContentPage.ToolbarItems>
    <ToolbarItem x:Name="tbItemNavigateMap" Text="Done" />
  </ContentPage.ToolbarItems>

    <TableView Intent="Settings" HasUnevenRows="true">
      <TableSection Title="Manual location" x:Name="TableSectionFilters">
        <ViewCell>
          <ViewCell.View>
            <Grid Padding="16,6,16,6" ColumnSpacing="16">
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*"/>
              <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Label
                   Text="{Binding ChangeLocationText}"
                   VerticalTextAlignment="Center"
                   HorizontalOptions="StartAndExpand"
                   Style="{DynamicResource NearbyListItemTextStyle}" />

            <Switch Grid.Column="1"
                    VerticalOptions="Center"
                    IsToggled="{Binding ChangeLocationIsEnabled}"/>

            <StackLayout IsVisible="{Binding ChangeLocationIsEnabled}" Grid.Row="1" Grid.ColumnSpan="2" HorizontalOptions="FillAndExpand" Orientation="Vertical" Spacing="{StaticResource SmallSpacing}">
              <Entry Placeholder="Latitude" Text="{Binding CustomLatitude}" HeightRequest="45" HorizontalOptions="FillAndExpand"></Entry>

              <Entry Placeholder="Longitude" Text="{Binding CustomLongitude}" HeightRequest="45" HorizontalOptions="FillAndExpand"></Entry>
            </StackLayout>

            

          </Grid>
        </ViewCell.View>
        </ViewCell>
      </TableSection>
    </TableView>

    <control:CardView Padding="0,32,0,0">
      <StackLayout Spacing="0">
        <control:HeaderDivider/>
        <Grid Padding="16,0" BackgroundColor="White">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
          </Grid.ColumnDefinitions>

          <Entry Grid.Row="2" Grid.ColumnSpan="2" Placeholder="Search location" Text="{Binding CustomLocation}" HeightRequest="45" HorizontalOptions="FillAndExpand"></Entry>
          
        </Grid>
      </StackLayout>
    </control:CardView>

      <!--<StackLayout>

      <control:LabelSection Text="Manual location"/>
      <control:HeaderDivider/>
        
      
      
    </StackLayout>-->
  
</ContentPage>